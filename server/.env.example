DB_HOST=127.0.0.1
DB_PORT=3306
# 建议使用专用账号，只授予本库权限，不要直接用 root
DB_USER=todo_app
DB_PASSWORD=换成你的数据库密码
DB_NAME=todo_items

# 后端监听端口。前端构建产物由本服务托管，直接用 http://IP:8218 访问
PORT=8218

# 同源部署不需要跨域，留空即禁止跨域；仅当前后端分域时填写允许的来源
CORS_ORIGIN=

MAX_UPLOAD_BYTES=10485760
